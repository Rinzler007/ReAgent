# ReAgent
It is a self-hosted tool that records agent runs (LLM calls, tool calls, state transitions) as OpenTelemetry traces, lets you replay any past run with a modified prompt/model/tool and diffs the new run against the original.
